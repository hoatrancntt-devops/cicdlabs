name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub with Access Token
        # DOCKERHUB_TOKEN should be a Docker Hub access token, not your password
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./nodejs-app
          push: true
          tags: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/nodejs-app:latest

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Deploy to Kubernetes
        env:
          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
        run: |
          echo "$KUBE_CONFIG_DATA" | base64 -d > kubeconfig
          export KUBECONFIG=$PWD/kubeconfig
          kubectl set image deployment/nodejs-app nodejs-app=docker.io/${{ secrets.DOCKERHUB_USERNAME }}/nodejs-app:latest --namespace=default || kubectl apply -f nodejs-app/k8s/deployment.yaml
          kubectl apply -f nodejs-app/k8s/service.yaml
